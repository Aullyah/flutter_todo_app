import 'package:flutter/material.dart';

const errorMessage = SnackBar(content: Text('Something went wrong'));
const inProgressMessage = SnackBar(content: Text('In Progress'));
var totalTask = 0;
var totalBusiness = 0;
var totalPersonal = 0;
